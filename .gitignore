# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*~

# Logs & temp
*.log
*.tmp
.cache/
# Headless test/boot run output (Start-Process redirect targets — see docs/dev-setup.md).
/out.txt
/err.txt

# Dependencies / build output (fill in as the stack is chosen)
node_modules/
dist/
build/
out/

# Environment / secrets
.env
.env.*
!.env.example

# Godot 4
# Editor import cache & metadata — regenerated on open, never commit.
.godot/
# Mono/.NET build output (only if a C# module is ever added).
.mono/
data_*/
# Exported builds.
/build/
# Export presets can hold signing keys / store passwords — keep out of git.
export_presets.cfg
